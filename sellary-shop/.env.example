# API base URL for the Sellary backend.
# Leave blank in dev — Vite proxies /api → http://localhost:8001 automatically.
# Set in production/staging to the full backend origin (e.g. https://api.example.com).
VITE_API_BASE_URL=
